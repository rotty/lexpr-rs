[package]
name = "msrv-test"
version = "0.0.1"
authors = ["Andreas Rottmann <a.rottmann@gmx.at>"]
edition = "2021"
rust-version = "1.71" # MSRV: Due to proc-macro2

[dependencies]
lexpr = { path = "../lexpr", features = ["sexp-macro"] }

[workspace]
